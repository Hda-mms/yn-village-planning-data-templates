# 填写完成自检清单

- [ ] `GHDLTBTZ.shp` 文件名、字段名、类型和长度未改动，`.shp/.shx/.dbf/.prj/.cpg` 齐全。
- [ ] 已选择与原规划成果目标年规划数据集一致的 33/34/35 带模板。
- [ ] 所有 `GHDLTBTZ` 要素几何有效、无正面积重叠，必填字段不为空。
- [ ] `BXT` 全部为“全量包”；`GHTZFS` 与 `FHQX` 分类一致。
- [ ] `TZHGHDLMC` 使用固定对照表中的名称；特定地类建设强度字段已填全。
- [ ] `FHQX=180/290` 时，每个要素 `BZ` 只有一个有效 305 编码。
- [ ] 涉 152 时，建新区图层的 `XZQDM/XZQMC` 每个要素均已填写且相互对应。
- [ ] 涉 152 时，使用单 PDF 或 Markdown 已准备；Markdown 每页六字段齐全，面积带单位。
- [ ] 输入目录没有旧版、备份、副本、样例或上次资料，避免形成多个候选。
- [ ] 原规划成果包保持原目录结构和只读副本；填写资料与成果资料未混放。
